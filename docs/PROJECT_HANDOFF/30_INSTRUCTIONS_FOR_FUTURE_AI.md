# Instructions for the Future AI

You are inheriting a long-running quantitative ML research system. Do not assume incomplete work is
broken and do not restart expensive computation until you understand the existing checkpoints.

1. Read `PROJECT_CONTEXT.md`, `AGENTS.md`, and every document in this package before modifying or
   running Phase 7A.
2. Treat repository source, configs, tests, manifests, checkpoint metadata, reports, Git history,
   and verified control-plane facts as evidence. If unsupported, write `UNKNOWN / NOT VERIFIED`.
3. The project is on hold. Do not start/resize/create cloud compute or train without explicit owner
   authorization and a fresh cost/deadline contract.
4. Preserve causal rules: completed-candle availability, `open[i+2]`, actual `label_end_time`
   purge, 120-minute embargo, chronological folds, train-only preprocessing, Cal-A/Cal-B, and
   frozen TEST identity.
5. Preserve July unused and August permanently `LOCKED_UNUSED`, `used=false`, and
   `evaluation_authorized=false`. Never inspect it to make a development decision.
6. Preserve Core20 unless the owner explicitly authorizes a new versioned methodology. Keep
   historically eligible delisted HNT; do not force XPIN or use future information.
7. Never fabricate, interpolate, synthesize, or forward-fill missing official market history.
   Distinguish lifecycle absence from interior active-period gaps and preserve source lineage.
8. Validate checkpoints and hashes before recomputing. Reuse valid discovery, acquisition, 60
   candle checkpoints, and Gold 7/7. Do not mistake presence or partial output for COMPLETE.
9. Fix/profile the Fold-1 preparation/checkpoint bottleneck before another expensive Fold 1. Make
   the smallest general change and prove cache identity/equivalence.
10. Do not confuse poor model performance with a code error—or incomplete training with poor
    performance. Current Phase 7A qualification is not evaluable.
11. Do not reduce folds, symbols, features, horizons, architectures, purge/embargo, or NO_TRADE for
    speed unless the owner approves a separately identified scientific experiment.
12. Cost safety is part of correctness. Never reset runtime budgets silently; maintain one worker,
    independent shutdown enforcement, durable checkpoints, and no false COMPLETE state.
13. Test targeted behavior and the complete Linux suite, Ruff, identities, holdout, and diff before
    resuming. Stop on P0/P1 evidence.
14. Never run legacy trading files, access an account, submit orders, start Phase 8, or promise
    profitability. Risk may veto ML; leverage is never a feature.
15. Preserve unrelated/untracked owner artifacts and shared Git history. No destructive recovery,
    force push, or blanket staging.

Your first substantive task is not training. It is to verify the frozen state and engineer an
atomic scientifically safe Fold-1 prepared-matrix checkpoint with observability.
