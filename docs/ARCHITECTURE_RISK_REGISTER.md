# Architecture risk register

Phase 7.1 audit date: 2026-08-24. Severity is impact if ignored; status is the
state after this audit. `BLOCKED` means the cloud baseline must not start.

| ID | Severity | Finding | Evidence | Status / required action |
|---|---:|---|---|---|
| R-01 | Critical | Acquisition-union symbols could influence model-facing cross-sectional percentiles and breadth before causal fold admission. | `generate_multiasset_features` originally used registry-active rows from the eventual acquisition union. | RESOLVED in `multiasset_features_v2`: fold slicing now rebinds all universe-dependent features to frozen `fold_active_symbols`; preview rows are explicitly marked non-model-ready. |
| R-02 | High | P0 estimator eligibility used Cal-A plus Cal-B row counts, allowing the policy-selection segment to determine model availability. | `_run_one` concatenated both calibration segments. | RESOLVED: P0 uses TRAIN, Validation, and Cal-A only. Cal-B owns thresholds/policy selection only. |
| R-03 | Critical | `multiasset_targets_v1` uses the exact next 5m open, whose timestamp equals `feature_time`. A completed-candle signal cannot guarantee a fill at that already-occurring price. | Phase 6 label engine sets `entry_time=open_time[i+1]` and `feature_time=open_time[i]+5m`; they are equal. | RESOLVED for new Phase 7 research by `multiasset_targets_v2`: one full 5m decision-latency bar makes `entry_time=feature_time+5m`, horizons start at entry, and timing/gap/cutoff/purge tests pass. V1 remains an audit-only reproducibility path; no old artifact was overwritten. |
| R-04 | Medium | The Phase 7 target config has 15m, 30m, 1h, and 2h, while the audit request also names 4h. | `TargetConfig.horizons_minutes=(15,30,60,120)`. | OPEN. Treat 4h as unsupported, not silently omitted or added. Add only as a versioned experiment after approval. |
| R-05 | Medium | H0 residual corrections are fitted on Validation, increasing selection/overfit pressure even though TEST is untouched. | `hybrid_calibration=validation`. | OPEN. Keep as a declared baseline; compare against no-correction H0 and nested TRAIN-only residual fitting before promotion. |
| R-06 | High | Multiple fold/model comparisons inflate false-discovery risk. | 16 folds, two views, four architectures, feature ablations, horizons, targets, and weighting/ID variants. | OPEN. Apply family definitions, stationary/block bootstrap, SPA/Reality Check and Deflated Sharpe recommendations before champion claims. |
| R-07 | High | Historical Fear & Greed knowledge time is not verified and Binance OI is recent/forward-only. | Context manifests and guards. | CONTROLLED. Both remain disabled from Phase 7 model features; no fabricated backfill. |
| R-08 | High | Research costs are assumptions, not an executable fill model; funding/latency/impact may be understated. | Cost tiers model fee/spread/slippage but no order-book queue, latency or realized funding path. | OPEN. Results must remain research-only; run stress grids and later event-driven shadow simulation. |
| R-09 | High | There is no durable authoritative execution ledger or live reconciliation engine. | Current repository intentionally contains no live execution implementation. | CONTROLLED BY SCOPE. Pure order/position/reconciliation transitions now pass all 35 required race simulations, but they are not a database, adapter, or safety approval. Implement durability and exchange integration only in a later authorized phase. |
| R-10 | Medium | A single final TEST segment per fold does not by itself establish stationarity or calibrated uncertainty. | Current metrics are point estimates plus fold reports. | OPEN. Add dependence-aware confidence intervals, forecast comparison tests and drift monitoring recommendations. |
| R-11 | Critical | July 2026 or the August holdout could invalidate the prospective design if touched. | Configuration and Gold boundary assertions. | LOCKED: cutoff is exclusive 2026-07-01; July is unused; August begins 2026-08-01 and remains `LOCKED_UNUSED`, unauthorized. |
| R-12 | Medium | External frameworks are not directly performance-comparable because data, targets, costs and splits differ. | Benchmark evidence is architectural documentation, not common-dataset experiments. | CONTROLLED. Scores are capability/evidence scores only; no return/Sharpe comparison is made. |
| R-13 | High | The new canonical artifact inventory matches legacy file/byte counts, and all modification times predate the task, but its aggregate digest does not reproduce the earlier undocumented ad-hoc digest. | Canonical v1: `ad44fe62...`; legacy report: `11a00ed1...`; common encoding variants did not reproduce legacy; no artifact path appears in the Git diff. | `RESOLVED_BY_REVIEWER_ACCEPTANCE_OF_CANONICAL_FINGERPRINT_V1` on 2026-08-25. Canonical v1 is authoritative going forward. The old undocumented digest is retained only as historical metadata and is not directly comparable with v1. |
| R-14 | High | Later services could assign incompatible meanings to models, predictions, events, adapters, portfolio state, or dashboard projections. | Phase 7.1 identified documentation-level boundaries but no shared typed/versioned contract package. | RESOLVED for the foundation: `crypto_ai.contracts` adds semantic versions, unique schema fingerprints, deterministic identities, typed envelopes and explicit ownership. Future breaking changes require a new major contract. |
| R-15 | Critical | PAPER/SHADOW/LIVE abstractions could be mistaken for execution authorization or allow UI code to become a safety authority. | A protocol name alone does not constrain adapter capabilities or state ownership. | CONTROLLED: adapter capabilities are explicit; the sole concrete adapter is `DisabledExecutionAdapter` with no I/O or private/write capability; dashboard contracts are query-only. No live adapter or command API exists. |
| R-16 | Critical | A future-proofing refactor could silently change the approved Phase 7 scientific baseline. | Phase 7.1 was approved in a dirty working tree, so commit identity alone cannot freeze its exact scientific state. | RESOLVED by `phase7_scientific_baseline_v1`: the approved Phase 7 source/config files are byte-frozen by SHA-256 and safe CLI outputs are contract-tested. Any drift is a `PHASE7_1B_CORRECTNESS_BLOCKER`, not an automatic fingerprint update. |

## Cloud gate

The target, universe, feature, split, calibration, economic-timing, holdout,
and artifact-preservation controls pass after the fixes and reviewer decision.
The gate is **READY FOR CLOUD BASELINE**. Open research risks R-04 through R-10
remain disclosure, stress-testing, and later-promotion requirements; they do
not authorize a champion or live system. Cloud execution remains manual and is
not performed by this audit.
